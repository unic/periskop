test_name: Graceful VM Restart
bot_name: roboto
timeout: 180
slack:
  channel: "#stackaton"
  as_user: roberterdin
  text: "!restart infra-dev-rer.p.unic24.net integration-test"
expect:
  regex: true
  attachments:
    text: |
      .*\([0-9]+s elapsed\).*